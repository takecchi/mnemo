{
  "name": "@mnemora/openai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EmbeddingProvider / LLMProvider の OpenAI 実装。zod スキーマを OpenAI の Structured Output (response_format: json_schema) へ翻訳する（docs/architecture.md §3.8）。",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@mnemora/core": "workspace:*",
    "openai": "7.10.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "5.0.0"
  }
}
